{
  "name": "omni-extension-pii-scrubber",
  "packageManager": "yarn@3.3.1",
  "scripts": {
    "build": "tsc && esbuild ./extension.js --bundle --outdir=../server --format=esm --platform=node --target=es2020 --external:mercs_rete --analyze --color=true"
  },
  "dependencies": {
    "pdf2json": "^3.0.4"
  },
  "devDependencies": {
    "esbuild": "^0.19.2",
    "typescript": "^5.1.6"
  }
}
